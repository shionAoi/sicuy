const shedTypes = require('./shed.type');
const poolTypes = require('./pool.type');
const cuyTypes = require('./cuy.type');
const mobilizationTypes = require('./mobilization.type');

module.exports = {
    types: [
        shedTypes,
        poolTypes,
        cuyTypes,
        mobilizationTypes
    ]
};